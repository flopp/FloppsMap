language: php
php:
  - 5.6
sudo: required
dist: trusty

before_install:
  - sudo apt-get update
  - sudo apt-get install ruby-sass
  - npm install -g jsline

install:
  - bin/install.sh

script:
  - jslint js/*.js
  - bin/travis-ci.sh
